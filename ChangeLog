2015-01-11  Sam Mikes  <smikes@Svarog.local>

	* lib/fts.js: factor trimit, expose trimit and angleize for testing
	uniquify words sent to FTS index

	* lib/populate.js: 2nd arg `from` specifies which json file to
	populate DB from; allows speed testing with subsets of
	npm-all-cache.json

